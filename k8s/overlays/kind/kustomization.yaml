apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Image name ArgoCD will apply; you load this image into Kind (no registry needed).
images:
  - name: tax-tracker
    newTag: dev

